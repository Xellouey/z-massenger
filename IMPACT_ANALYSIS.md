# IMPACT ASSESSMENT

## Затронутые компоненты системы

### Прямо затронутые:
1. **Модуль звонков (Calls)**
   - `lib/screens/app_screens/pick_up_call/` - экран принятия звонка
   - `lib/controllers/app_pages_controllers/audio_call_controller.dart` - управление аудио звонками
   - `lib/controllers/app_pages_controllers/video_call_controller.dart` - управление видео звонками (вероятно та же проблема)

2. **Модуль уведомлений**
   - `lib/controllers/common_controllers/notification_controller.dart` - обработка FCM
   - Android notification channels

3. **Модуль чатов**
   - `lib/controllers/app_pages_controllers/chat_controller.dart` - управление экраном чата
   - `lib/screens/app_screens/chat_message/` - UI чата

4. **Навигационная система**
   - GetX navigation (routing)
   - Передача аргументов между экранами

### Косвенно затронутые:
1. **User Experience (UX)**
   - Восприятие приложения пользователями
   - Доверие к качеству продукта

2. **Групповые звонки**
   - Если есть такая функциональность, она может иметь те же проблемы

3. **Broadcast звонки**
   - Аналогично групповым

---

## Масштаб влияния проблем

### ПРОБЛЕМА #1: Неверное отображение звонящего

**Затронутые пользователи:** 100% пользователей получающих входящие аудио звонки

**Сценарии использования:**
- ✅ Исходящие аудио звонки: работает правильно
- ❌ Входящие аудио звонки: всегда показывает свое имя
- ✅ Видео звонки (входящие/исходящие): работает правильно (есть корректная логика)
- ❓ Групповые звонки: не проверено, но вероятно проблема есть

**Частота воспроизведения:**
- 100% при входящих аудио звонках

**Влияние на бизнес:**
- 🟡 **Средняя серьезность**
- Пользователь видит неверное имя, но всё еще может принять/отклонить звонок
- Push уведомление показывает правильное имя, что помогает идентифицировать звонящего
- НЕ блокирует функциональность, но создает confusion

**Риски:**
- Пользователь может отклонить важный звонок, думая что это его собственный тест
- Негативное впечатление о качестве приложения
- Потенциально пропущенные звонки

---

### ПРОБЛЕМА #2: Продолжение воспроизведения мелодии

**Затронутые пользователи:** 100% пользователей принимающих звонки

**Сценарии использования:**
- ❌ Принятие звонка через pickup screen: мелодия продолжает играть
- ✅ Отклонение звонка: вероятно работает (нужно проверить)
- ✅ Пропущенный звонок (timeout): вероятно работает

**Частота воспроизведения:**
- 100% при принятии звонка

**Влияние на бизнес:**
- 🔴 **Высокая серьезность (UX критическая)**
- Серьезное нарушение пользовательского опыта
- Раздражает пользователя во время разговора
- Требует workaround (открытие/закрытие шторки)

**Риски:**
- Пользователь может завершить звонок раньше времени из-за раздражения
- Невозможность нормально общаться из-за фоновой мелодии
- Негативные отзывы в магазинах приложений
- **Потенциальная утечка батареи** - звук играет в фоне
- **Проблемы с приватностью** - звук может продолжать играть в публичных местах

**Побочные эффекты:**
- Возможное влияние на другие звуковые потоки (медиа, игры)
- Конфликт с системными звуками

---

### ПРОБЛЕМА #3: Crash + Неверная навигация

**Затронутые пользователи:** 100% пользователей завершающих звонки через AudioCallController

**Сценарии использования:**
- ❌ Завершение аудио звонка через controller: CRASH
- ❓ Завершение видео звонка: не проверено, но вероятно та же проблема
- ❓ Отклонение звонка через pickup screen: использует другую функцию (_navigateToChat в pick_up_body)
- ❓ Завершение группового звонка: не проверено

**Частота воспроизведения:**
- 100% при завершении звонка через AudioCallController.onCallEnd()

**Влияние на бизнес:**
- 🔴 **Критическая серьезность**
- **ПРИЛОЖЕНИЕ КРАШИТСЯ** - полная потеря функциональности
- Пользователь теряет контекст после звонка
- Возможна потеря несохраненных данных (если есть)

**Риски:**
- Пользователь не может продолжить работу с приложением после звонка
- Негативное восприятие стабильности приложения
- Возможные негативные отзывы: "приложение крашится после каждого звонка"
- **Критический user flow нарушен**

**Даже если crash исправлен:**
- Неверная навигация (открытие чата вместо dashboard) нарушает UX
- Пользователь ожидает вернуться к списку чатов
- Аналогично WhatsApp/Telegram/Viber

---

## Оценка рисков при исправлении

### ПРОБЛЕМА #1: Исправление отображения имени

**Сложность исправления:** 🟢 Низкая (1-2 строки кода)

**Потенциальные риски:**
- ✅ **Минимальные риски**
- Изменение затрагивает только UI отображение
- Не влияет на бизнес-логику

**Затронутые области:**
- `lib/screens/app_screens/pick_up_call/pick_up_body.dart:363`

**Тестирование:**
- ✓ Проверить входящие аудио звонки
- ✓ Проверить исходящие аудио звонки (не должны сломаться)
- ✓ Проверить видео звонки (уже работают правильно, не должны сломаться)
- ✓ Проверить групповые звонки (если есть)

**Edge cases:**
- Что если callerName = null или пустая строка?
- Что если это групповой звонок? (есть логика для isGroup)
- Что если callerId == receiverId? (звонок самому себе - не должно быть возможно, но стоит проверить)

---

### ПРОБЛЕМА #2: Отмена notification при принятии звонка

**Сложность исправления:** 🟡 Средняя

**Потенциальные риски:**
- 🟡 **Средние риски**
- Нужно получить доступ к FlutterLocalNotificationsPlugin из pick_up_body
- Нужно знать ID уведомления для отмены
- Может потребовать рефакторинг архитектуры (dependency injection)

**Затронутые области:**
- `lib/screens/app_screens/pick_up_call/pick_up_body.dart:124-199`
- `lib/controllers/common_controllers/notification_controller.dart` (возможно нужен публичный метод)

**Варианты решения:**
1. **Простой (но не идеальный):**
   ```dart
   // Отменить ВСЕ уведомления
   await flutterLocalNotificationsPlugin.cancelAll();
   ```
   - ⚠️ Риск: отменит и другие уведомления (сообщения и т.д.)

2. **Правильный:**
   ```dart
   // Отменить конкретное уведомление по ID
   await flutterLocalNotificationsPlugin.cancel(notificationId);
   ```
   - Требует передачи ID из notification_controller в pick_up_body
   - Более сложная архитектура, но безопаснее

3. **Альтернативный:**
   - Создать метод в NotificationController: `cancelCallNotification()`
   - Вызывать его из pick_up_body
   - Инкапсулирует логику отмены

**Тестирование:**
- ✓ Принять звонок → мелодия должна остановиться
- ✓ Отклонить звонок → проверить что не сломалось
- ✓ Пропустить звонок → проверить что не сломалось
- ✓ Получить сообщение во время звонка → его уведомление не должно отмениться
- ✓ Проверить на Android разных версий (поведение notification может отличаться)

**Edge cases:**
- Что если пользователь уже вручную смахнул уведомление?
- Что если пришло несколько уведомлений о звонке (duplicate)?
- Что если notification service не инициализирован?

---

### ПРОБЛЕМА #3: Исправление crash + навигации

**Сложность исправления:** 🟡 Средняя

**Потенциальные риски:**
- 🟡 **Средние риски**
- Изменения затрагивают критический user flow
- Нужно изменить навигацию после звонка (может повлиять на ожидания пользователей)

**Вариант 1: Минимальное исправление (предотвратить crash)**

Файл: `lib/controllers/app_pages_controllers/chat_controller.dart:319`

```dart
// БЫЛО:
if (data["message"] != null && data['message'] != "") {

// ДОЛЖНО БЫТЬ:
if (data != null &&
    data.containsKey("message") &&
    data["message"] != null &&
    data["message"] != "") {
```

**Риски:**
- ✅ Минимальные
- Только добавляется проверка безопасности
- Не меняет поведение при наличии ключа

**Тестирование:**
- ✓ Навигация с полными аргументами (с 'message')
- ✓ Навигация без 'message' (не должно крашнуться)
- ✓ Навигация с null данными
- ✓ Проверить строку 337 (forwardMessage) - та же проблема

**Вариант 2: Правильное исправление (изменить навигацию)**

Файл: `lib/controllers/app_pages_controllers/audio_call_controller.dart:807-810`

```dart
// БЫЛО:
log("endCall: Cleanup complete, navigating to chat");
await _navigateToChat();

// ДОЛЖНО БЫТЬ:
log("endCall: Cleanup complete, navigating to dashboard");
Get.back(); // Закрыть экран звонка
await Future.delayed(const Duration(milliseconds: 100));
Get.offAllNamed(routeName.dashboard); // Вернуться на dashboard
```

**Риски:**
- 🟡 Средние
- Меняет UX после звонка
- Пользователи привыкли к текущему поведению? (вряд ли, т.к. оно крашится)
- Нужно проверить не сломаются ли другие flows

**Затронутые файлы:**
- `lib/controllers/app_pages_controllers/audio_call_controller.dart`
- `lib/controllers/app_pages_controllers/video_call_controller.dart` (вероятно та же проблема)
- `lib/screens/app_screens/pick_up_call/pick_up_body.dart` (оставить как есть? или тоже изменить?)

**Тестирование:**
- ✓ Завершить аудио звонок → должен вернуться на dashboard
- ✓ Завершить видео звонок → аналогично
- ✓ Отклонить звонок через pickup screen → проверить поведение (возможно нужно изменить)
- ✓ Проверить не потеряются ли черновики сообщений (если были)
- ✓ Проверить состояние navigation stack

**Вариант 3: Полное решение (унификация)**

Создать единый сервис навигации:

```dart
class CallNavigationService {
  static Future<void> navigateAfterCallEnd({
    required Call call,
    CallEndReason reason = CallEndReason.normal,
  }) async {
    // Логика навигации в одном месте
    Get.back(); // Закрыть call screen
    await Future.delayed(const Duration(milliseconds: 100));
    Get.offAllNamed(routeName.dashboard);
  }

  static Future<void> navigateAfterCallDecline({
    required Call call,
    bool shouldOpenChat = false,
  }) async {
    // Логика для отклонения
    // ...
  }
}
```

**Риски:**
- 🔴 Высокие (много изменений)
- Требует рефакторинга нескольких файлов
- Нужно протестировать все сценарии звонков
- Высокая вероятность регрессий

**Рекомендация:** Использовать Вариант 1 + Вариант 2 как промежуточное решение, Вариант 3 - для будущего рефакторинга.

---

## Похожие проблемы в других частях кода

### Потенциальные проблемы в video_call_controller.dart

**Файл не был полностью проанализирован**, но вероятны те же проблемы:

1. **Навигация после видео звонка:**
   - Вероятно есть похожий метод `_navigateToChat()`
   - Может иметь ту же проблему с аргументами

2. **Отображение информации звонящего:**
   - Для видео звонков в pick_up_body.dart уже ЕСТЬ правильная логика (строки 224-228)
   - Но стоит проверить другие места

**Рекомендация:** Провести аудит video_call_controller.dart перед релизом исправлений.

---

### Другие места использования небезопасного доступа к Map

**Файл:** `lib/controllers/app_pages_controllers/chat_controller.dart`

**Строка 337-348:**
```dart
if (data["forwardMessage"] != null && data['forwardMessage'] != "") {
  // ❌ Та же проблема!
```

**Рекомендация:** Добавить проверку `data.containsKey("forwardMessage")`.

---

### Общий паттерн небезопасного кода

**Поиск в кодовой базе:**
Рекомендуется найти все места где используется паттерн:
```dart
data["key"] != null
```

Без предварительной проверки:
```dart
data.containsKey("key")
```

**Команда для поиска:**
```bash
grep -r 'data\["' lib/controllers/ lib/screens/
```

---

## Влияние на тестирование

### Регрессионное тестирование после исправлений

**Минимальный набор тестов:**

1. **Звонки:**
   - [ ] Входящий аудио звонок (принять)
   - [ ] Входящий аудио звонок (отклонить)
   - [ ] Исходящий аудио звонок
   - [ ] Входящий видео звонок (принять)
   - [ ] Входящий видео звонок (отклонить)
   - [ ] Исходящий видео звонок
   - [ ] Групповой звонок (если есть)
   - [ ] Пропущенный звонок
   - [ ] Звонок во время другого звонка

2. **Навигация:**
   - [ ] После завершения аудио звонка
   - [ ] После завершения видео звонка
   - [ ] После отклонения звонка
   - [ ] Переход в чат из других мест (не должен сломаться)
   - [ ] Навигация с forwardMessage
   - [ ] Навигация с message parameter

3. **Уведомления:**
   - [ ] Звонок в foreground
   - [ ] Звонок в background
   - [ ] Звонок когда app terminated
   - [ ] Несколько уведомлений подряд
   - [ ] Отмена уведомления при принятии
   - [ ] Сохранение других уведомлений (сообщений)

4. **Edge cases:**
   - [ ] Звонок от неизвестного номера
   - [ ] Звонок от заблокированного пользователя
   - [ ] Потеря сети во время звонка
   - [ ] Низкий заряд батареи
   - [ ] Разрешения камеры/микрофона отозваны

---

## Приоритизация исправлений

### Критичность × Сложность матрица:

| Проблема | Критичность | Сложность | Приоритет |
|---|---|---|---|
| #3: Crash при навигации | 🔴 Критическая | 🟡 Средняя | **P0 - СРОЧНО** |
| #2: Мелодия не останавливается | 🔴 Высокая (UX) | 🟡 Средняя | **P0 - СРОЧНО** |
| #3: Неверная навигация (UX) | 🟡 Средняя | 🟡 Средняя | **P1 - Важно** |
| #1: Неверное отображение | 🟡 Средняя | 🟢 Низкая | **P1 - Важно** |
| Архитектура: Унификация навигации | 🟡 Средняя | 🔴 Высокая | **P2 - Желательно** |

### Рекомендуемый порядок исправлений:

**Sprint 1 (Критические баги):**
1. ✅ Исправить crash в ChatController (добавить проверки ключей)
2. ✅ Остановка мелодии при принятии звонка
3. ✅ Изменить навигацию после звонка на dashboard

**Sprint 2 (Важные улучшения):**
4. ✅ Исправить отображение имени звонящего
5. ✅ Провести аудит video_call_controller.dart
6. ✅ Исправить аналогичные проблемы (forwardMessage и т.д.)

**Sprint 3 (Рефакторинг):**
7. ✅ Создать единый CallNavigationService
8. ✅ Добавить типизацию для navigation arguments
9. ✅ Написать unit/integration тесты для call flow

---

## Потенциальные регрессии

### Риск №1: Изменение навигации может сломать другие flows

**Сценарий:**
- Пользователь нажимает на уведомление о пропущенном звонке
- Ожидает: открытие чата с этим пользователем
- Если мы всегда навигируем на dashboard, этот flow сломается

**Митигация:**
- Передавать параметр `source` в navigation
- Если source == "notification", открывать чат
- Если source == "call_end", открывать dashboard

---

### Риск №2: Отмена всех уведомлений может убрать важные

**Сценарий:**
- Пользователь получил несколько сообщений
- Получил звонок
- Принял звонок
- Если мы вызовем `cancelAll()`, уведомления о сообщениях тоже удалятся

**Митигация:**
- Использовать `cancel(specificId)` вместо `cancelAll()`
- Хранить ID уведомления в глобальном state
- Или: использовать фиксированный ID для call notifications

---

## Метрики для мониторинга после релиза

### Crash rate:
- До исправления: X% (измерить текущий)
- После исправления: должен упасть до ~0%
- Метрика: Firebase Crashlytics "NoSuchMethodError in ChatController.getChatData"

### User engagement после звонка:
- До: сколько пользователей продолжают использовать app после звонка?
- После: должно увеличиться (т.к. нет crash)

### Время до следующего действия после звонка:
- До: долгое (нужно перезапустить app)
- После: быстрое (остаются на dashboard)

### Отзывы в магазинах:
- Мониторить упоминания: "crashes after call", "sound keeps playing", "wrong name"
